// eslint-disable-next-line no-unused-vars
import React from 'react'

const UseCard = (props) => {
  return (
    <>
    <div>use card component</div>
    <h4>name is {props.myname}</h4>
    <h4>age is {props.myage}</h4>
    <h4>location is {props.mylocation}</h4>
    </>
  )
}

export default UseCard
